---
import BaseLayout from '../layouts/BaseLayout.astro'
import '../styles/global.css'
const pageTitle = "About Me";

const identity = {
	firstName: "bruh",
	country: "bruhland",
	occupation: "bruhming",
	hobbies: ["bruhf", "bruhd", "bruhs"],
};

const skills = ['none', 'uhhhh', 'shrugh']

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase"
---
<BaseLayout pageTitle={pageTitle}>
	<head>
		<style define:vars={{skillColor, fontWeight, textCase}}>
		.skill {
			color: var(--skillColor);
			font-weight: var(--fontWeight);
			text-transform: var(--textCase);
		}
		</style>
	</head>
        <h2>and uhhhh fjffartjdkjf</h2>
		<p>Here are a few facts about me:</p>
		<ul>
			<li>My name is {identity.firstName}.</li>
			<li>I live in {identity.country} and I work as a {identity.occupation}.</li>
			{identity.hobbies.length >= 2 &&
				<li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li>
			}
		</ul>
		<p>My skills are:</p>
		<ul>
			{skills.map((skill) => <li class="skill">{skill}</li>)}
		</ul>

        <p>i am working on deez nuts this is the seoncdjfisj</p>
        <p>this wuhjfdifjidbisfisighfdogjidfs </p>
		{happy && <p>I am happy to be learning dee</p>}
		{finished && <p>finsih da tut</p>}
		{goal == 3 ? <p>my goal is to finish in 3 days</p> : <p>my goal is not 3 days but 3 years !!!!!</p>}
</BaseLayout>